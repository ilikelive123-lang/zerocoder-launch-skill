# Путь к файлу OAuth-клиента Google (по умолчанию scripts/credentials.json).
# Раскомментируй и укажи свой путь, только если хранишь credentials.json в другом месте.
# GOOGLE_CREDENTIALS_PATH=scripts/credentials.json
